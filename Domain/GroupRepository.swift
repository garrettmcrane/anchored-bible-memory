import Foundation

struct GroupRepository {
    static let shared = GroupRepository()

    // TODO: Add local group persistence when group features ship.
    // TODO: Add remote sync and account-aware data access when backend work begins.
}
